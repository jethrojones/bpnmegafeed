{
  "name": "bpn-mega-feed",
  "version": "0.1.0",
  "description": "Combine podcast RSS feeds into one network feed while preserving original audio URLs.",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "node src/build-feed.js",
    "feeds:transistor": "node src/update-feeds-from-transistor.js",
    "start": "node src/server.js",
    "test": "node --test"
  },
  "dependencies": {
    "fast-xml-parser": "^5.7.2"
  }
}
