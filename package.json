{
  "name": "mega-podcast-rss",
  "version": "0.1.0",
  "description": "Combine podcast RSS feeds into one network feed while preserving original audio URLs.",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "node src/build-feed.js",
    "start": "node src/server.js",
    "test": "node --test"
  },
  "dependencies": {
    "fast-xml-parser": "^4.5.3"
  }
}
